#include <stdio.h>

int main()
{
    char str[20];
    scanf("%[^\n]s",str);
    for(int i=0;i<str[i];i++){
        if(i==0)
        str[i] = str[i]-32;
        else if(str[i]>=65 && str[i]<=90)
            str[i]=str[i]+32;}
        
        printf("%s\n",str);
    
    
    return 0;
}
